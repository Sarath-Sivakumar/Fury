<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Fury" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/theme_green</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/app_theme_secondary</item>
        <item name="colorSecondaryVariant">@color/app_theme_third</item>
        <!--Color of item inside floating action button-->
        <item name="colorOnSecondary">@color/total_lite</item>
         <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/Status_bar</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Customize your theme here. -->
        <item name="android:fontFamily">@font/raleway_semibold</item>

    </style>
</resources>